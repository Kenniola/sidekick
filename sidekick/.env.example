# .env (never commit this file)

# ---------------------------------------------------------------------------
# LLM Auth — Sidekick uses Copilot API (primary) + GitHub Models (fallback)
# Both use the same GitHub token. Normally auto-detected via `gh auth token`.
# ---------------------------------------------------------------------------
# Only set this if gh CLI is unavailable:
# GITHUB_TOKEN=<your-github-pat>

# ---------------------------------------------------------------------------
# Azure Speech (optional — for speaker diarization)
# Preferred: configure in ~/.sidekick/customers.yaml instead of env vars.
# ---------------------------------------------------------------------------
# AZURE_SPEECH_ENDPOINT=https://<resource>.cognitiveservices.azure.com/
# AZURE_SPEECH_KEY=<your-key>
